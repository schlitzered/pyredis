# Project Rules

- Docstrings (but not inline comments) are allowed and encouraged for public, client-facing classes, methods, and functions (such as Clients, Pools, Commands, and `get_by_url`) to support API documentation generation.
